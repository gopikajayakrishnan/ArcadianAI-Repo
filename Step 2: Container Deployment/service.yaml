apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-loadbalancer.fullname" . }}
  labels:
    {{- include "nginx-loadbalancer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "nginx-loadbalancer.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      name: http
